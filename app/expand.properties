# NOTE: not provided by the user — build.gradle's processResources reads this file to
# expand @token@ placeholders in application.yml. Filled with placeholder values so the
# resource-filtering step doesn't fail outright; replace with real deployment values.
serverAddress=0.0.0.0
serverServletContextPath=platform-management
subsystemCode=CHANGE_ME
ufsChannel=CHANGE_ME
appDeploymentUnit=CHANGE_ME
ufsRelease=CHANGE_ME
