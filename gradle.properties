# NOTE: not provided by the user — added because settings.gradle references
# `enableMavenLocalRepository` and would fail to evaluate without it. Confirm the
# real value used in your actual repo.
enableMavenLocalRepository=false
